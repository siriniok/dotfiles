#!/bin/bash

# Origin: https://github.com/bradp/dotfiles/blob/master/bin/loglive

while :
do
    clear
    git --no-pager log --graph --pretty=oneline --abbrev-commit --decorate --all $*
    sleep 1
done
